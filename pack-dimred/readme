编辑run.sh，jar后面加三个参数
1.向量存放路径（给路径即可，不是文件）
2.模型(km->k均值，bkm->二分k均值)
3.一次聚类簇数量
-----------------------------------------------
产生三个结果
1.$path/events：嫌疑事件名单
2.$path/vecs：用于聚类的向量（带标签）
3.$path/$modelStr/eva：二次聚类结果
4.$path/$modelStr/checklist：玩家id,运营标签，预测值
